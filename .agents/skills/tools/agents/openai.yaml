interface:
  display_name: "Tools"
  short_description: "Repo-specific patterns and workflows for tools"
  default_prompt: "Use the tools repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true